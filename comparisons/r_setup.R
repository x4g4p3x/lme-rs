# Loaded by cross-language benchmark R scripts (see scripts/run_cross_language_benchmarks.py).
# lme4::predict() uses rlang's %||% operator; attach rlang so predict() works under Rscript.
suppressPackageStartupMessages(library(rlang))
