# TODO

- [ ] **Configure PyPI Trusted Publishing for `lme_python` GitHub Actions:**
  If you want the PyPI publishing to work:
  1. Go to your PyPI account > Manage Account > Publishing.
  2. Add a new GitHub Publisher.
  3. Link it to your GitHub owner name, the `lme-rs` repository name, and specify `pypi` as the environment name.
